export {
  StackedModal,
  ModalStackProvider,
  ModalStackProvider as StackedModalProvider,
  useModalStack,
  useModalStack as useStackedModal,
  ModalContent,
  ModalActions,
} from './StackedModal';

export type { StackedModalProps } from './StackedModal';

export { default } from './StackedModal';
